name: Post-Push Workflow

# Trigger the workflow on push events
on:
  push:
    branches:
      - main  # You can change this to any branch you are working on

jobs:
  post-push-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository content
        uses: actions/checkout@v2

      - name: Display a message for post-push
        run: echo "Post-push action triggered!"